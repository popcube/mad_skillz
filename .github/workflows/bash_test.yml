name: bash test
on: workflow_dispatch

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'TZ-9: '$(TZ=-9 date +"%Y/%m/%d %H%M%S")
          echo 'TZ-0: '$(date +"%Y/%m/%d %H%M%S")
          TZ=-9 date +"%Y/%m/%d %H%M%S"
          TZ=JST-9 date +"%Y/%m/%d %H%M%S"
